{
  "name": "student-late-tracking-system",
  "version": "1.0.0",
  "description": "Full-stack Student Late Tracking System",
  "scripts": {
    "start": "cd backend && npm start",
    "build": "cd backend && npm install",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}